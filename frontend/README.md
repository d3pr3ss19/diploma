# Frontend (React + Vite + TypeScript + Ant Design)

## Запуск

```bash
npm install
npm run dev
```

Приложение поднимается по адресу `http://localhost:5173`.

## Маршруты MVP

- `/login` — страница входа
- `/dashboard` — панель
- `/subscribers` — список абонентов
- `/requests` — список заявок
- `/account` — просмотр лицевого счёта по `subscriberId`

## API

HTTP-клиент настроен в `src/api/http.ts` с базовым URL:

`http://localhost:3000/api/v1`


## Что уже реализовано (Шаг 3.2)

- Форма логина отправляет `POST /auth/login`.
- Токен и пользователь сохраняются в `localStorage`.
- Для защищённых роутов подключён `RequireAuth`.
- Axios автоматически добавляет `Authorization: Bearer <token>`.


## Что уже реализовано (Шаг 3.3)

- Страница `Абоненты` загружает данные через `GET /subscribers`.
- Страница `Заявки` загружает данные через `GET /requests`.
- Страница `Лицевой счёт` получает данные через `GET /subscribers/:id` и показывает связанные счета.


## Что уже реализовано (Шаг 4.1)

- На странице `Абоненты` добавлена модальная форма создания (`POST /subscribers`).
- На странице `Заявки` добавлена модальная форма создания (`POST /requests`).
- После успешного создания список автоматически обновляется.


## Что уже реализовано (Шаг 4.2)

- Добавлена фронтенд-валидация UUID полей в формах создания.
- Для телефона абонента добавлена валидация формата `+380XXXXXXXXX`.
- Добавлены уведомления об успешном создании абонента/заявки.


## Что уже реализовано (Шаг 4.3)

- В формах создания автоматически подставляется UUID текущего пользователя (`userId` / `createdByUserId`).
- Добавлен разбор backend-ошибок из Axios (`message`/`error`) для более понятных сообщений.
- Основной layout переведён на светлый стиль (light sidebar + светлый фон).


## Что уже реализовано (Шаг 4.4)

- В карточке `Лицевой счёт` показываются сводные показатели по заявкам (всего/открытые/выполнено).
- Добавлен блок с последними заявками, связанными с лицевыми счетами абонента.
- Для ошибок загрузки карточки используется человекочитаемый разбор API-ответов.


## Что уже реализовано (Шаг 5.1)

- В `Абоненты` добавлены поиск и сортировка (по дате и по ФИО).
- В `Заявки` добавлены поиск, фильтр по статусу и сортировка по дате.
- Фильтрация применяется клиент-сайд поверх загруженных данных.


## Что уже реализовано (Шаг 5.2)

- Для списков `Абоненты` и `Заявки` добавлена пагинация.
- Параметры поиска/фильтров/сортировки сохраняются в URL query (`q`, `sort`, `status`, `page`).
- Состояние списков можно открыть по ссылке с уже применёнными фильтрами.


## Что уже реализовано (Шаг 5.3)

- В списках добавлены кнопки быстрого сброса фильтров.
- Добавлены операторские пресеты фильтров (например, `новые`, `в работе`, `по алфавиту`).
- Пресеты изменяют URL-параметры, поэтому ими можно делиться ссылкой.


## Что уже реализовано (Шаг 5.4)

- Выбранные пресеты списков сохраняются в `localStorage`.
- При повторном входе последний пресет автоматически применяется, если URL не содержит фильтров.
- Кнопка сброса фильтров очищает и URL, и сохранённый пресет.


## Что уже реализовано (Шаг 6.1)

- Добавлен модуль `src/utils/list-filters.ts` с чистыми функциями фильтрации/сортировки/пагинации.
- Добавлен базовый набор unit-тестов `src/utils/list-filters.spec.ts` (Vitest).
- Страницы `Абоненты` и `Заявки` переведены на использование этих утилит.
